# Each WorkItem has one current assignee

A WorkItem has at most one current Work Assignee, whether assigned explicitly or established by an Agent Claim. Parallel responsibility is explicitly modeled through related or Child WorkItems and an optional aggregation WorkItem, while reviewers and conversational Agent Requests remain separate from assignment; mentioning multiple Agents never changes WorkItem ownership or implicitly creates work.
