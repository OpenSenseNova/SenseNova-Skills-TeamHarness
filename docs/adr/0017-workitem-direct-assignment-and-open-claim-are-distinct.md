# WorkItem Direct Assignment and open Claim are distinct

Direct Assignment explicitly gives a WorkItem to a named member, while open Claim lets an eligible Agent autonomously choose a Claimable WorkItem. Either path may create an Agent Request after responsibility is established, but neither changes the shared Run and Attempt model; a conversational `@Agent` is a separate directed Agent Request and never counts as Direct Assignment.
