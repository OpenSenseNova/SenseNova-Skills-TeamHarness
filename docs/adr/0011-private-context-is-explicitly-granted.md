# Private context is explicitly granted

A team-triggered Shared Agent execution uses its shared Run Context Snapshot by default and may use local private context only through an Owner-approved Private Context Grant. Private content remains local, read permission is separate from permission to disclose information in shared output, audit records only source categories and policy versions rather than full content, and local credentials are consumed through controlled tools rather than placed in a Runtime Prompt.
