# Completion is governed by Work Policy

An ordinary Run completes by committing its final Message and has no Acceptance semantics. An explicitly managed WorkItem may use that Message and its Artifacts as a Result Submission under an optional, versioned Completion Policy that completes low-risk work automatically or requires an authorized reviewer or verifier; rejection creates a new Agent Request and Run within the same WorkItem rather than turning every conversation into hidden approval workflow.
