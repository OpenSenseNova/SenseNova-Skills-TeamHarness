# WorkItem boundaries are explicit

Only explicit work-management actions create, advance, split, assign, or delegate a WorkItem. Agent Mentions create conversational Agent Requests even inside a WorkItem discussion; a caller may explicitly associate a request with an existing WorkItem or create a Child WorkItem, but neither Workspace heuristics nor a language model may infer hidden work state from message wording.
