# Agent identity is independent of Runtime

An Agent is a stable Workspace member whose identity, Owner, permissions, capabilities, authorship, and collaboration history do not depend on a particular Runtime. A Host may replace a Runtime Binding, including switching between ACP-compatible or other adapters, without creating a new Agent; Runtime-specific sessions and protocol objects remain local execution state and never become Workspace identity or domain authority.
